import { FusenDirective } from './fusen.directive';

describe('FusenDirective', () => {
  it('should create an instance', () => {
    const directive = new FusenDirective();
    expect(directive).toBeTruthy();
  });
});
